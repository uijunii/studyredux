import React from 'react';
import { SafeAreaView, Text, View } from 'react-native';

const App = () => {
  
  const num = 0;

  return (
    <SafeAreaView>
      <Text>{num}</Text>
    </SafeAreaView>
  );

};

export default App;
